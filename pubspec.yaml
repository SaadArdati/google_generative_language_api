name: google_generative_language_api
description: A Rest API wrapper client for Google's Generative Language API.
version: 0.2.0
repository: https://github.com/SaadArdati/google_generative_language_api

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.1.0
  json_annotation: ^4.8.1
  equatable: ^2.0.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.2
  dotenv: ^4.1.0
